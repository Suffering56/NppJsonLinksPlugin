necessary: 
    ﻿- придумать как отлавливать добавление/удаление строк (источников ссылок) и фиксировать это: строки просто в истории какой-то хранить, а ссылки удалять 
    + найти либу по работе с Json-ами 
    - понять какой нужен будет формат маппинга
    - окно настроек:
        - enabled/disabled
        - mapping
    
    + do fix: при открытии любого файла происходит вызов SCN_MODIFIED, который нужно игнорировать

unnecessary:
    - do fix: многократный вызов NPPN_BUFFERACTIVATED при релоаде файла (если файл изменили за пределами NPP)
    - do fix: при "прыжке" выделяется текст (от текущей позиции курсора до линии на которую прыгнули)
    - do fix: почему-то на 64-битной версии NPP notification.ModificationType всегда = 0, и вообще как-то там плохо парсится ScNotification
